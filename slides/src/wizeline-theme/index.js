import theme from "mdx-deck/themes";
// import DefaultSlide from "./components/DefaultSlide";

export default {
  // extends the default theme
  ...theme,

  // add a custom font
  font: "Oswald, Roboto, sans-serif"

  // In case you need to tweak the master slide
  // Provider: DefaultSlide
};
